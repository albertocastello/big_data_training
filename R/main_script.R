# install packages
#install.packages(c("data.table", "purrr", "furrr", "sf", "parallel", "arrow"), dependencies = TRUE)

# load packages
library(data.table)
library(purrr)
library(furrr)
library(sf)
library(parallel)
library(arrow)